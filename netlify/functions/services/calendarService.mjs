// Stage 4: will create a real Google Calendar event via Calendar API
// Replace this stub with the real implementation

export async function createCalendarEvent(booking) {
    console.log('[CalendarService] MOCK – would create event on:', booking.date, 'for:', booking.name);
    // TODO Stage 4: connect Google Calendar API with Service Account
}
