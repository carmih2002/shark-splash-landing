// Stage 3: will send real email via Nodemailer + Gmail
// Replace this stub with the real implementation

export async function sendBookingEmail(booking) {
    console.log('[EmailService] MOCK – would send email for:', booking.name, booking.date);
    // TODO Stage 3: connect Nodemailer + Gmail App Password
}
